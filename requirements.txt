# Use the requirements.txt from the Udacity workspace if it differs.
# These are the versions this guide's snippets were written against.
smolagents>=1.14.0
openai>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
tabulate>=0.9.0
